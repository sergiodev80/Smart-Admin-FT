# CRM Demo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffold `apps/crm_demo` — una app demo que exhibe los 12 componentes UI del proyecto en un flujo CRM real (contactos, historial, HTMX).

**Architecture:** Una sola vista split panel en `/admin/crm-demo/contacts/` actúa como hub central. Las vistas parciales HTMX manejan el detalle, inline edit, modal de creación y eliminación. El admin estándar de Unfold cubre CRUD completo con tabs e inlines.

**Tech Stack:** Django 6, Unfold Admin, HTMX, Alpine.js, PostgreSQL, `apps.notifications`, `apps.audit`, `apps.config`

---

## File Map

| Archivo | Acción | Responsabilidad |
|---------|--------|-----------------|
| `apps/crm_demo/__init__.py` | Crear | Vacío |
| `apps/crm_demo/apps.py` | Crear | AppConfig con plugin_urls/middleware/settings |
| `apps/crm_demo/models.py` | Crear | Contact, Purchase, EmailLog, CallLog, SocialProfile |
| `apps/crm_demo/services.py` | Crear | ContactService.create / register_purchase |
| `apps/crm_demo/admin.py` | Crear | ContactAdmin con AuditMixin, inlines, bulk action |
| `apps/crm_demo/urls.py` | Crear | 7 rutas + @staff_member_required |
| `apps/crm_demo/views.py` | Crear | ContactListView, partials HTMX, InlineEditView, DeleteContactView |
| `apps/crm_demo/dashboard.py` | Crear | get_crm_cards, get_recent_contacts_table |
| `apps/crm_demo/migrations/0001_initial.py` | Crear | Migración inicial (auto) |
| `apps/crm_demo/migrations/0002_default_config.py` | Crear | Inserta crm_demo_contact_max=500 |
| `apps/crm_demo/templates/crm_demo/contact_list.html` | Crear | Página principal: page_header + stat_cards + filter_tabs + split_panel + toast |
| `apps/crm_demo/templates/crm_demo/contact_list_items.html` | Crear | Partial izquierdo: lista + inline_edit + empty_state |
| `apps/crm_demo/templates/crm_demo/contact_detail.html` | Crear | Partial derecho: skeleton + data_list + copy_to_clipboard + empty_state por historial |
| `apps/crm_demo/templates/crm_demo/contact_create_modal.html` | Crear | Partial modal: modal_content + form |
| `apps/crm_demo/tests/__init__.py` | Crear | Vacío |
| `apps/crm_demo/tests/test_models.py` | Crear | Tests de modelos |
| `apps/crm_demo/tests/test_services.py` | Crear | Tests de ContactService |
| `apps/crm_demo/tests/test_views.py` | Crear | Tests de vistas |
| `config/settings/base.py` | Modificar | Agregar `"apps.crm_demo"` a LOCAL_APPS + sidebar + dashboard |

---

## Task 1: App skeleton + AppConfig

**Files:**
- Create: `apps/crm_demo/__init__.py`
- Create: `apps/crm_demo/apps.py`

- [ ] **Step 1: Crear `__init__.py` vacío**

```python
# apps/crm_demo/__init__.py
```

- [ ] **Step 2: Crear `apps.py`**

```python
# apps/crm_demo/apps.py
import os

from django.apps import AppConfig
from django.utils.translation import gettext_lazy as _


class CrmDemoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.crm_demo"
    verbose_name = _("CRM Demo")

    plugin_urls = [
        {"prefix": "admin/crm-demo/", "urlconf": "apps.crm_demo.urls"},
    ]
    plugin_middleware = []
    plugin_settings = {}
```

- [ ] **Step 3: Commit**

```bash
git add apps/crm_demo/__init__.py apps/crm_demo/apps.py
git commit -m "feat(crm_demo): add app skeleton and AppConfig"
```

---

## Task 2: Models

**Files:**
- Create: `apps/crm_demo/models.py`
- Test: `apps/crm_demo/tests/test_models.py`

- [ ] **Step 1: Crear `tests/__init__.py`**

```python
# apps/crm_demo/tests/__init__.py
```

- [ ] **Step 2: Escribir tests de modelos (TDD — deben fallar)**

```python
# apps/crm_demo/tests/test_models.py
import pytest
from django.db import IntegrityError

from apps.crm_demo.models import Contact, SocialProfile


@pytest.mark.django_db
def test_contact_status_default():
    contact = Contact.objects.create(name="Ana", email="ana@test.com")
    assert contact.status == "lead"


@pytest.mark.django_db
def test_contact_status_valid_choices():
    for status in ("lead", "prospect", "client", "inactive"):
        c = Contact.objects.create(name="X", email=f"{status}@test.com", status=status)
        assert c.status == status


@pytest.mark.django_db
def test_social_profile_unique_per_network():
    contact = Contact.objects.create(name="Bob", email="bob@test.com")
    SocialProfile.objects.create(contact=contact, network="linkedin", url="https://linkedin.com/in/bob")
    with pytest.raises(IntegrityError):
        SocialProfile.objects.create(contact=contact, network="linkedin", url="https://linkedin.com/in/bob2")
```

- [ ] **Step 3: Ejecutar tests — deben fallar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_models.py -v
```
Expected: `ImportError` o `ModuleNotFoundError` — los modelos no existen aún.

- [ ] **Step 4: Crear `models.py`**

```python
# apps/crm_demo/models.py
from django.db import models
from django.utils.translation import gettext_lazy as _


class Contact(models.Model):
    STATUS_LEAD = "lead"
    STATUS_PROSPECT = "prospect"
    STATUS_CLIENT = "client"
    STATUS_INACTIVE = "inactive"
    STATUS_CHOICES = [
        (STATUS_LEAD, _("Lead")),
        (STATUS_PROSPECT, _("Prospecto")),
        (STATUS_CLIENT, _("Cliente")),
        (STATUS_INACTIVE, _("Inactivo")),
    ]

    name       = models.CharField(_("nombre"), max_length=200)
    email      = models.EmailField(_("email"), unique=True)
    phone      = models.CharField(_("teléfono"), max_length=50, blank=True)
    company    = models.CharField(_("empresa"), max_length=200, blank=True)
    status     = models.CharField(_("estado"), max_length=20, choices=STATUS_CHOICES, default=STATUS_LEAD)
    created_at = models.DateTimeField(_("creado el"), auto_now_add=True)

    class Meta:
        verbose_name = _("Contacto")
        verbose_name_plural = _("Contactos")
        ordering = ["-created_at"]

    def __str__(self):
        return f"{self.name} <{self.email}>"


class Purchase(models.Model):
    contact     = models.ForeignKey(Contact, on_delete=models.CASCADE, related_name="purchases", verbose_name=_("contacto"))
    amount      = models.DecimalField(_("monto"), max_digits=10, decimal_places=2)
    description = models.CharField(_("descripción"), max_length=300)
    date        = models.DateField(_("fecha"))

    class Meta:
        verbose_name = _("Compra")
        verbose_name_plural = _("Compras")
        ordering = ["-date"]

    def __str__(self):
        return f"{self.contact.name} — ${self.amount}"


class EmailLog(models.Model):
    DIRECTION_IN = "in"
    DIRECTION_OUT = "out"
    DIRECTION_CHOICES = [
        (DIRECTION_IN, _("Entrante")),
        (DIRECTION_OUT, _("Saliente")),
    ]

    contact   = models.ForeignKey(Contact, on_delete=models.CASCADE, related_name="email_logs", verbose_name=_("contacto"))
    subject   = models.CharField(_("asunto"), max_length=200)
    body      = models.TextField(_("cuerpo"), blank=True)
    date      = models.DateTimeField(_("fecha"))
    direction = models.CharField(_("dirección"), max_length=3, choices=DIRECTION_CHOICES)

    class Meta:
        verbose_name = _("Email")
        verbose_name_plural = _("Emails")
        ordering = ["-date"]

    def __str__(self):
        return f"{self.subject} ({self.direction})"


class CallLog(models.Model):
    DIRECTION_IN = "in"
    DIRECTION_OUT = "out"
    DIRECTION_CHOICES = [
        (DIRECTION_IN, _("Entrante")),
        (DIRECTION_OUT, _("Saliente")),
    ]

    contact          = models.ForeignKey(Contact, on_delete=models.CASCADE, related_name="call_logs", verbose_name=_("contacto"))
    duration_minutes = models.PositiveIntegerField(_("duración (minutos)"))
    notes            = models.TextField(_("notas"), blank=True)
    date             = models.DateTimeField(_("fecha"))
    direction        = models.CharField(_("dirección"), max_length=3, choices=DIRECTION_CHOICES)

    class Meta:
        verbose_name = _("Llamada")
        verbose_name_plural = _("Llamadas")
        ordering = ["-date"]

    def __str__(self):
        return f"Llamada {self.direction} — {self.duration_minutes}min"


class SocialProfile(models.Model):
    NETWORK_LINKEDIN  = "linkedin"
    NETWORK_TWITTER   = "twitter"
    NETWORK_INSTAGRAM = "instagram"
    NETWORK_FACEBOOK  = "facebook"
    NETWORK_CHOICES = [
        (NETWORK_LINKEDIN,  "LinkedIn"),
        (NETWORK_TWITTER,   "Twitter"),
        (NETWORK_INSTAGRAM, "Instagram"),
        (NETWORK_FACEBOOK,  "Facebook"),
    ]

    contact = models.ForeignKey(Contact, on_delete=models.CASCADE, related_name="social_profiles", verbose_name=_("contacto"))
    network = models.CharField(_("red social"), max_length=20, choices=NETWORK_CHOICES)
    url     = models.URLField(_("URL"))

    class Meta:
        verbose_name = _("Perfil social")
        verbose_name_plural = _("Perfiles sociales")
        unique_together = [("contact", "network")]

    def __str__(self):
        return f"{self.contact.name} — {self.network}"
```

- [ ] **Step 5: Activar la app en settings**

Abrir `config/settings/base.py` y agregar `"apps.crm_demo"` a `LOCAL_APPS`:

```python
LOCAL_APPS = [
    "apps.core",
    "apps.notifications",
    "apps.audit",
    "apps.config",
    "apps.permissions",
    "apps.crm_demo",   # ← agregar
]
```

- [ ] **Step 6: Crear y aplicar migración inicial**

```bash
docker compose -f docker-compose.dev.yml exec web_dev python manage.py makemigrations crm_demo
docker compose -f docker-compose.dev.yml exec web_dev python manage.py migrate
```

Expected: `Applying crm_demo.0001_initial... OK`

- [ ] **Step 7: Ejecutar tests — deben pasar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_models.py -v
```
Expected: 3 tests PASSED.

- [ ] **Step 8: Commit**

```bash
git add apps/crm_demo/models.py apps/crm_demo/tests/__init__.py apps/crm_demo/tests/test_models.py apps/crm_demo/migrations/ config/settings/base.py
git commit -m "feat(crm_demo): add models — Contact, Purchase, EmailLog, CallLog, SocialProfile"
```

---

## Task 3: Data migration — config key

**Files:**
- Create: `apps/crm_demo/migrations/0002_default_config.py`

- [ ] **Step 1: Crear la migración manual**

```python
# apps/crm_demo/migrations/0002_default_config.py
from django.db import migrations

CRM_CONFIGS = [
    {
        "key": "crm_demo_contact_max",
        "value": "500",
        "value_type": "int",
        "description": "Número máximo de contactos permitidos en la demo.",
        "is_public": False,
    },
]


def create_configs(apps, schema_editor):
    SiteConfig = apps.get_model("config", "SiteConfig")
    for cfg in CRM_CONFIGS:
        SiteConfig.objects.get_or_create(key=cfg["key"], defaults=cfg)


def delete_configs(apps, schema_editor):
    SiteConfig = apps.get_model("config", "SiteConfig")
    SiteConfig.objects.filter(key__in=[c["key"] for c in CRM_CONFIGS]).delete()


class Migration(migrations.Migration):
    dependencies = [
        ("crm_demo", "0001_initial"),
        ("config", "0002_default_config"),
    ]

    operations = [
        migrations.RunPython(create_configs, reverse_code=delete_configs),
    ]
```

- [ ] **Step 2: Aplicar migración**

```bash
docker compose -f docker-compose.dev.yml exec web_dev python manage.py migrate crm_demo
```
Expected: `Applying crm_demo.0002_default_config... OK`

- [ ] **Step 3: Commit**

```bash
git add apps/crm_demo/migrations/0002_default_config.py
git commit -m "feat(crm_demo): data migration — crm_demo_contact_max config key"
```

---

## Task 4: ContactService

**Files:**
- Create: `apps/crm_demo/services.py`
- Test: `apps/crm_demo/tests/test_services.py`

- [ ] **Step 1: Escribir tests de services (TDD — deben fallar)**

```python
# apps/crm_demo/tests/test_services.py
import pytest
from unittest.mock import patch

from apps.crm_demo.models import Contact, Purchase
from apps.crm_demo.services import ContactService


@pytest.mark.django_db
def test_create_contact_sends_in_app_notification(superuser):
    with patch("apps.crm_demo.services.NotificationService.send") as mock_send:
        contact = ContactService.create(
            {"name": "Ana", "email": "ana@test.com", "status": "lead"},
            created_by=superuser,
        )
    assert contact.pk is not None
    mock_send.assert_called_once()
    call_kwargs = mock_send.call_args
    assert "in_app" in call_kwargs.kwargs.get("channels", call_kwargs.args[3] if len(call_kwargs.args) > 3 else [])


@pytest.mark.django_db
def test_create_contact_raises_when_max_exceeded(superuser):
    with patch("apps.crm_demo.services.ConfigService.get", return_value=1):
        Contact.objects.create(name="Existing", email="existing@test.com")
        with pytest.raises(ValueError, match="máximo"):
            ContactService.create(
                {"name": "New", "email": "new@test.com"},
                created_by=superuser,
            )


@pytest.mark.django_db
def test_register_purchase_sends_email_notification(superuser):
    contact = Contact.objects.create(name="Bob", email="bob@test.com")
    with patch("apps.crm_demo.services.NotificationService.send") as mock_send:
        purchase = ContactService.register_purchase(
            contact,
            {"amount": "99.99", "description": "Producto A", "date": "2026-01-01"},
            created_by=superuser,
        )
    assert purchase.pk is not None
    mock_send.assert_called_once()
    call_kwargs = mock_send.call_args
    channels = call_kwargs.kwargs.get("channels", call_kwargs.args[3] if len(call_kwargs.args) > 3 else [])
    assert "email" in channels
```

Nota: `superuser` es un fixture de pytest-django. Agregar `conftest.py`:

```python
# apps/crm_demo/tests/conftest.py
import pytest
from django.contrib.auth import get_user_model

User = get_user_model()


@pytest.fixture
def superuser(db):
    return User.objects.create_superuser(
        username="admin_test",
        email="admin@test.com",
        password="testpass123",
    )
```

- [ ] **Step 2: Ejecutar tests — deben fallar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_services.py -v
```
Expected: `ImportError` — services.py no existe.

- [ ] **Step 3: Crear `services.py`**

```python
# apps/crm_demo/services.py
import logging
from decimal import Decimal

from django.utils.translation import gettext_lazy as _

from apps.config.services import ConfigService
from apps.notifications.services import NotificationService

from .models import Contact, Purchase

logger = logging.getLogger(__name__)


class ContactService:
    @staticmethod
    def create(data: dict, created_by) -> Contact:
        current_count = Contact.objects.count()
        max_contacts = ConfigService.get("crm_demo_contact_max", default=500)
        if current_count >= max_contacts:
            raise ValueError(
                f"Se alcanzó el máximo de {max_contacts} contactos permitidos en la demo."
            )

        contact = Contact.objects.create(
            name=data["name"],
            email=data["email"],
            phone=data.get("phone", ""),
            company=data.get("company", ""),
            status=data.get("status", Contact.STATUS_LEAD),
        )

        NotificationService.send(
            user=created_by,
            title=str(_("Nuevo contacto creado")),
            body=str(_("Se creó el contacto %(name)s (%(email)s).") % {
                "name": contact.name, "email": contact.email
            }),
            channels=["in_app"],
        )
        return contact

    @staticmethod
    def register_purchase(contact: Contact, data: dict, created_by) -> Purchase:
        purchase = Purchase.objects.create(
            contact=contact,
            amount=Decimal(str(data["amount"])),
            description=data["description"],
            date=data["date"],
        )

        NotificationService.send(
            user=created_by,
            title=str(_("Nueva compra registrada")),
            body=str(_("%(name)s realizó una compra de $%(amount)s.") % {
                "name": contact.name, "amount": purchase.amount
            }),
            channels=["email"],
        )
        return purchase
```

- [ ] **Step 4: Ejecutar tests — deben pasar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_services.py -v
```
Expected: 3 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add apps/crm_demo/services.py apps/crm_demo/tests/test_services.py apps/crm_demo/tests/conftest.py
git commit -m "feat(crm_demo): add ContactService with notifications"
```

---

## Task 5: Admin

**Files:**
- Create: `apps/crm_demo/admin.py`

- [ ] **Step 1: Crear `admin.py`**

```python
# apps/crm_demo/admin.py
from django.contrib import admin
from django.utils.html import format_html
from django.utils.translation import gettext_lazy as _
from unfold.admin import ModelAdmin, TabularInline

from apps.audit.mixins import AuditMixin

from .models import CallLog, Contact, EmailLog, Purchase, SocialProfile


class PurchaseInline(TabularInline):
    model = Purchase
    extra = 0
    fields = ["date", "amount", "description"]


class EmailLogInline(TabularInline):
    model = EmailLog
    extra = 0
    fields = ["date", "direction", "subject"]


class CallLogInline(TabularInline):
    model = CallLog
    extra = 0
    fields = ["date", "direction", "duration_minutes", "notes"]


class SocialProfileInline(TabularInline):
    model = SocialProfile
    extra = 0
    fields = ["network", "url"]


STATUS_COLORS = {
    Contact.STATUS_LEAD:     ("bg-blue-100 text-blue-700",   "dark:bg-blue-900/30 dark:text-blue-300"),
    Contact.STATUS_PROSPECT: ("bg-yellow-100 text-yellow-700", "dark:bg-yellow-900/30 dark:text-yellow-300"),
    Contact.STATUS_CLIENT:   ("bg-green-100 text-green-700",  "dark:bg-green-900/30 dark:text-green-300"),
    Contact.STATUS_INACTIVE: ("bg-gray-100 text-gray-600",    "dark:bg-gray-800 dark:text-gray-400"),
}


@admin.register(Contact)
class ContactAdmin(AuditMixin, ModelAdmin):
    list_display   = ["name", "email", "company", "status_badge", "created_at"]
    list_filter    = ["status", "created_at"]
    search_fields  = ["name", "email", "company"]
    date_hierarchy = "created_at"
    warn_unsaved_changes = True

    fieldsets = [
        (
            _("Información general"),
            {
                "fields": ["name", "email", "phone", "company", "status"],
                "classes": ["tab"],
            },
        ),
        (
            _("Historial"),
            {
                "classes": ["tab"],
            },
        ),
    ]
    inlines = [PurchaseInline, EmailLogInline, CallLogInline, SocialProfileInline]
    actions = ["mark_inactive"]

    @admin.display(description=_("Estado"))
    def status_badge(self, obj):
        light, dark = STATUS_COLORS.get(obj.status, ("bg-gray-100 text-gray-600", ""))
        label = obj.get_status_display()
        return format_html(
            '<span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium {} {}">{}</span>',
            light, dark, label,
        )

    @admin.action(description=_("Marcar como inactivos"))
    def mark_inactive(self, request, queryset):
        updated = queryset.update(status=Contact.STATUS_INACTIVE)
        self.message_user(request, _("%(n)d contacto(s) marcados como inactivos.") % {"n": updated})
```

- [ ] **Step 2: Verificar que el admin carga sin errores**

```bash
docker compose -f docker-compose.dev.yml exec web_dev python manage.py check
```
Expected: `System check identified no issues (0 silenced).`

- [ ] **Step 3: Commit**

```bash
git add apps/crm_demo/admin.py
git commit -m "feat(crm_demo): add ContactAdmin with AuditMixin, inlines, status badge"
```

---

## Task 6: URLs y vistas — estructura base

**Files:**
- Create: `apps/crm_demo/urls.py`
- Create: `apps/crm_demo/views.py` (esqueleto — solo ContactListView funcional)
- Test: `apps/crm_demo/tests/test_views.py` (tests de acceso)

- [ ] **Step 1: Escribir tests de acceso (TDD)**

```python
# apps/crm_demo/tests/test_views.py
import pytest
from django.urls import reverse


@pytest.mark.django_db
def test_contact_list_requires_staff(client):
    url = reverse("crm_contact_list")
    response = client.get(url)
    assert response.status_code == 302  # redirect a login


@pytest.mark.django_db
def test_contact_list_accessible_for_staff(admin_client):
    url = reverse("crm_contact_list")
    response = admin_client.get(url)
    assert response.status_code == 200


@pytest.mark.django_db
def test_contact_items_partial(admin_client):
    url = reverse("crm_contact_items")
    response = admin_client.get(url)
    assert response.status_code == 200
```

- [ ] **Step 2: Ejecutar tests — deben fallar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_views.py -v
```
Expected: `NoReverseMatch` — las URLs no existen aún.

- [ ] **Step 3: Crear `urls.py`**

```python
# apps/crm_demo/urls.py
from django.urls import path

from . import views

urlpatterns = [
    path("contacts/",                      views.contact_list,        name="crm_contact_list"),
    path("contacts/items/",                views.contact_list_items,  name="crm_contact_items"),
    path("contacts/create/",               views.contact_create,      name="crm_contact_create"),
    path("contacts/counts/",               views.contact_counts,      name="crm_contact_counts"),
    path("contacts/<int:pk>/detail/",      views.contact_detail,      name="crm_contact_detail"),
    path("contacts/<int:pk>/inline-edit/", views.contact_inline_edit, name="crm_contact_inline_edit"),
    path("contacts/<int:pk>/delete/",      views.contact_delete,      name="crm_contact_delete"),
]
```

- [ ] **Step 4: Crear `views.py` — esqueleto funcional**

```python
# apps/crm_demo/views.py
import json

from django.contrib.admin.views.decorators import staff_member_required
from django.contrib import admin
from django.http import HttpResponse
from django.shortcuts import get_object_or_404, render
from django.utils import timezone
from django.utils.translation import gettext_lazy as _
from django.views.decorators.http import require_http_methods

from .models import CallLog, Contact, EmailLog, Purchase, SocialProfile
from .services import ContactService


def _admin_context(request):
    return admin.site.each_context(request)


@staff_member_required
def contact_list(request):
    from .dashboard import get_crm_cards
    status_filter = request.GET.get("status", "")
    qs = Contact.objects.all()
    if status_filter:
        qs = qs.filter(status=status_filter)

    tabs = [
        {
            "label": str(_("Todos")),
            "url": "?",
            "active": not status_filter,
            "count_url": "",
        },
        {
            "label": str(_("Leads")),
            "url": "?status=lead",
            "active": status_filter == "lead",
            "count_url": f"{request.path}counts/?status=lead",
            "count_type": "info",
        },
        {
            "label": str(_("Prospectos")),
            "url": "?status=prospect",
            "active": status_filter == "prospect",
            "count_url": f"{request.path}counts/?status=prospect",
            "count_type": "warning",
        },
        {
            "label": str(_("Clientes")),
            "url": "?status=client",
            "active": status_filter == "client",
            "count_url": f"{request.path}counts/?status=client",
            "count_type": "success",
        },
        {
            "label": str(_("Inactivos")),
            "url": "?status=inactive",
            "active": status_filter == "inactive",
            "count_url": f"{request.path}counts/?status=inactive",
        },
    ]

    context = _admin_context(request)
    context.update({
        "contacts": qs,
        "tabs": tabs,
        "status_filter": status_filter,
        "stat_cards": get_crm_cards(request),
    })
    return render(request, "crm_demo/contact_list.html", context)


@staff_member_required
def contact_list_items(request):
    status_filter = request.GET.get("status", "")
    qs = Contact.objects.all()
    if status_filter:
        qs = qs.filter(status=status_filter)
    return render(request, "crm_demo/contact_list_items.html", {"contacts": qs, "status_filter": status_filter})


@staff_member_required
def contact_counts(request):
    status = request.GET.get("status", "")
    count = Contact.objects.filter(status=status).count() if status else Contact.objects.count()
    return HttpResponse(str(count))


@staff_member_required
def contact_detail(request, pk):
    contact = get_object_or_404(Contact, pk=pk)
    fields = [
        {"label": str(_("Nombre")),  "value": contact.name},
        {"label": str(_("Email")),   "value": contact.email},
        {"label": str(_("Empresa")), "value": contact.company or "—"},
        {"label": str(_("Estado")),  "value": contact.get_status_display()},
        {"label": str(_("Teléfono")), "value": contact.phone or "—"},
        {"label": str(_("Creado")),  "value": contact.created_at.strftime("%d/%m/%Y %H:%M")},
    ]
    return render(request, "crm_demo/contact_detail.html", {
        "contact": contact,
        "fields": fields,
        "purchases": contact.purchases.all(),
        "email_logs": contact.email_logs.all(),
        "call_logs": contact.call_logs.all(),
        "social_profiles": contact.social_profiles.all(),
    })


@staff_member_required
@require_http_methods(["GET", "PATCH"])
def contact_inline_edit(request, pk):
    contact = get_object_or_404(Contact, pk=pk)
    allowed_fields = {"phone", "company"}

    if request.method == "PATCH":
        data = json.loads(request.body)
        field = data.get("field")
        value = data.get("value", "")
        if field not in allowed_fields:
            return HttpResponse(status=400)
        setattr(contact, field, value)
        contact.save(update_fields=[field])
        response = render(request, "unfold/components/inline_edit.html", {
            "value": getattr(contact, field),
            "field": field,
            "url": request.path,
            "field_type": "text",
        })
        response["HX-Trigger"] = json.dumps({
            "showToast": {"type": "success", "title": str(_("Guardado")), "body": ""}
        })
        return response

    field = request.GET.get("field", "phone")
    return render(request, "unfold/components/inline_edit.html", {
        "value": getattr(contact, field, ""),
        "field": field,
        "url": request.path,
        "field_type": "text",
    })


@staff_member_required
def contact_create(request):
    from django import forms as django_forms

    class ContactForm(django_forms.Form):
        name    = django_forms.CharField(max_length=200, label=str(_("Nombre")))
        email   = django_forms.EmailField(label=str(_("Email")))
        phone   = django_forms.CharField(max_length=50, required=False, label=str(_("Teléfono")))
        company = django_forms.CharField(max_length=200, required=False, label=str(_("Empresa")))
        status  = django_forms.ChoiceField(choices=Contact.STATUS_CHOICES, label=str(_("Estado")))

    if request.method == "POST":
        form = ContactForm(request.POST)
        if form.is_valid():
            try:
                ContactService.create(form.cleaned_data, created_by=request.user)
                response = HttpResponse(status=204)
                response["HX-Trigger"] = json.dumps({
                    "showToast": {"type": "success", "title": str(_("Contacto creado")), "body": ""},
                    "refreshContacts": {},
                })
                return response
            except ValueError as e:
                form.add_error(None, str(e))
        return render(request, "crm_demo/contact_create_modal.html", {"form": form})

    form = ContactForm()
    return render(request, "crm_demo/contact_create_modal.html", {"form": form})


@staff_member_required
@require_http_methods(["POST"])
def contact_delete(request, pk):
    contact = get_object_or_404(Contact, pk=pk)
    contact.delete()
    response = HttpResponse(status=204)
    response["HX-Trigger"] = json.dumps({
        "showToast": {"type": "success", "title": str(_("Contacto eliminado")), "body": ""},
    })
    response["HX-Redirect"] = "/admin/crm-demo/contacts/"
    return response
```

- [ ] **Step 5: Ejecutar tests de acceso — deben pasar**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/tests/test_views.py -v
```
Expected: 3 tests PASSED (los de create/inline-edit/delete se añaden en Task 8).

- [ ] **Step 6: Commit**

```bash
git add apps/crm_demo/urls.py apps/crm_demo/views.py apps/crm_demo/tests/test_views.py
git commit -m "feat(crm_demo): add views and URLs skeleton"
```

---

## Task 7: Templates

**Files:**
- Create: `apps/crm_demo/templates/crm_demo/contact_list.html`
- Create: `apps/crm_demo/templates/crm_demo/contact_list_items.html`
- Create: `apps/crm_demo/templates/crm_demo/contact_detail.html`
- Create: `apps/crm_demo/templates/crm_demo/contact_create_modal.html`

- [ ] **Step 1: Crear `contact_list.html`**

```django
{# apps/crm_demo/templates/crm_demo/contact_list.html #}
{% extends "unfold/page.html" %}
{% load i18n unfold %}

{% block title %}{% trans "Gestión de Contactos" %}{% endblock %}

{% block extrahead %}
{{ block.super }}
<script>
  document.addEventListener("refreshContacts", () => htmx.trigger("#contact-items", "refresh-list"));
</script>
{% endblock %}

{% block content %}

{# Toast — debe estar en el DOM desde el inicio #}
{% component "unfold/components/toast.html" %}{% endcomponent %}

{# Modal container #}
<div id="modal-container"
     x-data="{ openModal: false }"
     x-on:htmx:after-request.window="if($event.detail.elt.id==='modal-trigger') openModal=true">
  <div x-show="openModal" x-cloak
       style="position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:50;display:flex;align-items:center;justify-content:center"
       x-on:click.self="openModal=false">
    <div id="modal-body" style="z-index:51;min-width:28rem;max-width:90vw"></div>
  </div>
</div>

{# Page Header #}
{% url 'crm_contact_list' as list_url %}
{% component "unfold/components/page_header.html"
   with title=_("Gestión de Contactos")
        subtitle=_("Perfiles unificados con historial de compras, emails y llamadas")
        icon="contacts" %}
  <button id="modal-trigger"
          hx-get="{% url 'crm_contact_create' %}"
          hx-target="#modal-body"
          hx-swap="innerHTML"
          class="flex items-center gap-1.5 px-4 py-2 bg-primary-600 hover:bg-primary-700 text-white text-sm font-medium rounded-default transition-colors">
    <span class="material-symbols-outlined text-base leading-none">add</span>
    {% trans "Nuevo contacto" %}
  </button>
{% endcomponent %}

{# Stat Cards #}
<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1rem;margin-bottom:1.5rem">
  {% for card in stat_cards %}
    {% component "unfold/components/stat_card.html"
       with title=card.title value=card.value icon=card.icon %}
    {% endcomponent %}
  {% endfor %}
</div>

{# Filter Tabs #}
<div style="margin-bottom:1rem">
  {% component "unfold/components/filter_tabs.html" with tabs=tabs %}{% endcomponent %}
</div>

{# Split Panel #}
{% component "unfold/components/split_panel.html"
   with list_title=_("Contactos")
        detail_target="contact-detail"
        empty_icon="person"
        empty_title=_("Selecciona un contacto para ver su detalle") %}
  <div id="contact-items"
       hx-get="{% url 'crm_contact_items' %}{% if status_filter %}?status={{ status_filter }}{% endif %}"
       hx-trigger="load, refresh-list"
       hx-swap="innerHTML">
    {# Skeleton inicial #}
    {% for i in "xxxxx" %}
      {% component "unfold/components/skeleton.html" with height="56px" %}{% endcomponent %}
    {% endfor %}
  </div>
{% endcomponent %}

{% endblock %}
```

- [ ] **Step 2: Crear `contact_list_items.html`**

```django
{# apps/crm_demo/templates/crm_demo/contact_list_items.html #}
{% load i18n unfold %}

{% if contacts %}
  {% for contact in contacts %}
    <a href="#"
       hx-get="{% url 'crm_contact_detail' contact.pk %}"
       hx-target="#contact-detail"
       hx-swap="innerHTML"
       class="split-panel-item block">
      <div style="display:flex;align-items:center;justify-content:space-between;gap:0.5rem">
        <div>
          <span class="block font-medium text-sm text-gray-900 dark:text-white">{{ contact.name }}</span>
          <span class="block text-xs text-gray-500 dark:text-gray-400">{{ contact.email }}</span>
        </div>
        <div style="display:flex;flex-direction:column;align-items:flex-end;gap:0.25rem">
          {% if contact.company %}
            <span class="text-xs text-gray-400 dark:text-gray-500">{{ contact.company }}</span>
          {% endif %}
          {# Inline edit phone #}
          <div x-on:click.stop>
            {% component "unfold/components/inline_edit.html"
               with value=contact.phone
                    field="phone"
                    url=contact|url_inline_edit
                    placeholder=_("Teléfono")
                    empty_text=_("Sin teléfono") %}
            {% endcomponent %}
          </div>
        </div>
      </div>
    </a>
  {% endfor %}
{% else %}
  {% component "unfold/components/empty_state.html"
     with icon="search_off"
          title=_("Sin resultados")
          description=_("No hay contactos con este filtro.") %}
  {% endcomponent %}
{% endif %}
```

Nota: `contact|url_inline_edit` requiere un template filter personalizado. Ver step siguiente.

- [ ] **Step 3: Agregar template filter para URL de inline edit**

Crear `apps/crm_demo/templatetags/__init__.py` (vacío) y `apps/crm_demo/templatetags/crm_tags.py`:

```python
# apps/crm_demo/templatetags/crm_tags.py
from django import template
from django.urls import reverse

register = template.Library()


@register.filter
def url_inline_edit(contact):
    return reverse("crm_contact_inline_edit", kwargs={"pk": contact.pk})


@register.filter
def url_contact_delete(contact):
    return reverse("crm_contact_delete", kwargs={"pk": contact.pk})
```

Actualizar `contact_list_items.html` para cargar el tag:
```django
{% load i18n unfold crm_tags %}
```

- [ ] **Step 4: Crear `contact_detail.html`**

```django
{# apps/crm_demo/templates/crm_demo/contact_detail.html #}
{% load i18n unfold crm_tags %}

<div style="padding:1.5rem">

  {# Header del detalle #}
  <div style="display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:1.5rem">
    <div>
      <h2 class="text-lg font-semibold text-gray-900 dark:text-white">{{ contact.name }}</h2>
      <p class="text-sm text-gray-500 dark:text-gray-400">{{ contact.get_status_display }}</p>
    </div>
    {# Confirm dialog para eliminar — abre modal nativo de Unfold #}
    <div x-data="{ openModal: false }">
      <button
        type="button"
        x-on:click="openModal = true"
        class="flex items-center gap-1 px-3 py-1.5 text-xs font-medium text-red-600 border border-red-200 rounded-default hover:bg-red-50 dark:hover:bg-red-900/20 transition-colors">
        <span class="material-symbols-outlined text-sm leading-none">delete</span>
        {% trans "Eliminar" %}
      </button>
      <div x-show="openModal" x-cloak
           style="position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:50;display:flex;align-items:center;justify-content:center"
           x-on:click.self="openModal=false">
        <div style="z-index:51;width:28rem;max-width:90vw;background:white;border-radius:0.5rem"
             class="dark:bg-base-900">
          {% component "unfold/components/confirm_dialog.html"
             with title=_("Eliminar contacto")
                  body=_("Esta acción no se puede deshacer.")
                  confirm_text=contact.email
                  danger=True
                  action_url=contact|url_contact_delete %}
          {% endcomponent %}
        </div>
      </div>
    </div>
  </div>

  {# Data list — campos del contacto #}
  {% component "unfold/components/data_list.html" with fields=fields %}{% endcomponent %}

  {# Copy to clipboard — ID y email #}
  <div style="display:flex;gap:1rem;margin-top:1rem;margin-bottom:1.5rem">
    {% component "unfold/components/copy_to_clipboard.html"
       with value=contact.pk|stringformat:"s" label="ID" %}
    {% endcomponent %}
    {% component "unfold/components/copy_to_clipboard.html"
       with value=contact.email label="Email" truncate=True %}
    {% endcomponent %}
  </div>

  {# Historial de compras #}
  <div style="margin-bottom:1.5rem">
    <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
      <span class="material-symbols-outlined text-base align-middle mr-1">shopping_cart</span>
      {% trans "Compras" %}
    </h3>
    {% if purchases %}
      <div style="display:flex;flex-direction:column;gap:0.5rem">
        {% for purchase in purchases %}
          <div class="bg-base-50 dark:bg-base-800 rounded-default px-3 py-2 text-sm">
            <span class="font-medium">${{ purchase.amount }}</span>
            <span class="text-gray-500 mx-2">·</span>
            <span>{{ purchase.description }}</span>
            <span class="text-gray-400 text-xs ml-2">{{ purchase.date }}</span>
          </div>
        {% endfor %}
      </div>
    {% else %}
      {% component "unfold/components/empty_state.html"
         with icon="shopping_cart" title=_("Sin compras") %}
      {% endcomponent %}
    {% endif %}
  </div>

  {# Historial de emails #}
  <div style="margin-bottom:1.5rem">
    <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
      <span class="material-symbols-outlined text-base align-middle mr-1">email</span>
      {% trans "Emails" %}
    </h3>
    {% if email_logs %}
      <div style="display:flex;flex-direction:column;gap:0.5rem">
        {% for log in email_logs %}
          <div class="bg-base-50 dark:bg-base-800 rounded-default px-3 py-2 text-sm">
            <span class="font-medium">{{ log.subject }}</span>
            <span class="text-xs text-gray-400 ml-2">{{ log.get_direction_display }} · {{ log.date|date:"d/m/Y H:i" }}</span>
          </div>
        {% endfor %}
      </div>
    {% else %}
      {% component "unfold/components/empty_state.html"
         with icon="email" title=_("Sin emails") %}
      {% endcomponent %}
    {% endif %}
  </div>

  {# Historial de llamadas #}
  <div style="margin-bottom:1.5rem">
    <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
      <span class="material-symbols-outlined text-base align-middle mr-1">call</span>
      {% trans "Llamadas" %}
    </h3>
    {% if call_logs %}
      <div style="display:flex;flex-direction:column;gap:0.5rem">
        {% for log in call_logs %}
          <div class="bg-base-50 dark:bg-base-800 rounded-default px-3 py-2 text-sm">
            <span class="font-medium">{{ log.duration_minutes }} min</span>
            <span class="text-gray-500 mx-2">·</span>
            <span>{{ log.get_direction_display }}</span>
            {% if log.notes %}<span class="text-gray-400 text-xs ml-2">{{ log.notes }}</span>{% endif %}
            <span class="text-gray-400 text-xs ml-2">{{ log.date|date:"d/m/Y H:i" }}</span>
          </div>
        {% endfor %}
      </div>
    {% else %}
      {% component "unfold/components/empty_state.html"
         with icon="call" title=_("Sin llamadas") %}
      {% endcomponent %}
    {% endif %}
  </div>

  {# Redes sociales #}
  <div>
    <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
      <span class="material-symbols-outlined text-base align-middle mr-1">share</span>
      {% trans "Redes sociales" %}
    </h3>
    {% if social_profiles %}
      <div style="display:flex;gap:0.5rem;flex-wrap:wrap">
        {% for profile in social_profiles %}
          <a href="{{ profile.url }}" target="_blank"
             class="inline-flex items-center gap-1 px-3 py-1.5 text-xs font-medium bg-base-100 dark:bg-base-800 text-gray-700 dark:text-gray-300 rounded-default hover:bg-base-200 dark:hover:bg-base-700 transition-colors">
            <span class="material-symbols-outlined text-sm leading-none">link</span>
            {{ profile.get_network_display }}
          </a>
        {% endfor %}
      </div>
    {% else %}
      {% component "unfold/components/empty_state.html"
         with icon="share" title=_("Sin redes sociales") %}
      {% endcomponent %}
    {% endif %}
  </div>

</div>
```

- [ ] **Step 5: Crear `contact_create_modal.html`**

```django
{# apps/crm_demo/templates/crm_demo/contact_create_modal.html #}
{% load i18n unfold %}

{% component "unfold/components/modal_content.html"
   with title=_("Nuevo contacto") icon="person_add" size="md" %}

  <form hx-post="{% url 'crm_contact_create' %}"
        hx-swap="outerHTML"
        style="display:flex;flex-direction:column;gap:1rem">
    {% csrf_token %}

    {% if form.non_field_errors %}
      <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-default px-3 py-2">
        {% for error in form.non_field_errors %}
          <p class="text-sm text-red-600 dark:text-red-400">{{ error }}</p>
        {% endfor %}
      </div>
    {% endif %}

    {% for field in form %}
      <div>
        <label class="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">
          {{ field.label }}{% if field.field.required %} *{% endif %}
        </label>
        {{ field }}
        {% if field.errors %}
          {% for error in field.errors %}
            <p class="text-xs text-red-500 mt-1">{{ error }}</p>
          {% endfor %}
        {% endif %}
      </div>
    {% endfor %}

    <div style="display:flex;justify-content:flex-end;gap:0.75rem;padding-top:0.5rem">
      <button type="button" x-on:click="openModal=false"
              class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-base-100 dark:hover:bg-base-800 rounded-default transition-colors">
        {% trans "Cancelar" %}
      </button>
      <button type="submit"
              class="px-4 py-2 text-sm font-medium bg-primary-600 hover:bg-primary-700 text-white rounded-default transition-colors">
        {% trans "Crear contacto" %}
      </button>
    </div>
  </form>

{% endcomponent %}
```

- [ ] **Step 6: Verificar que el servidor arranca sin errores de template**

```bash
docker compose -f docker-compose.dev.yml exec web_dev python manage.py check --deploy 2>/dev/null || docker compose -f docker-compose.dev.yml exec web_dev python manage.py check
```
Expected: `System check identified no issues`.

- [ ] **Step 7: Commit**

```bash
git add apps/crm_demo/templates/ apps/crm_demo/templatetags/
git commit -m "feat(crm_demo): add all templates — split panel, detail, modal, list items"
```

---

## Task 8: Dashboard widget + Sidebar

**Files:**
- Create: `apps/crm_demo/dashboard.py`
- Modify: `config/settings/base.py`

- [ ] **Step 1: Crear `dashboard.py`**

```python
# apps/crm_demo/dashboard.py
from django.utils import timezone
from django.utils.translation import gettext_lazy as _

from apps.core.dashboard import _Table


def get_crm_cards(request):
    from .models import CallLog, Contact, EmailLog, Purchase

    today = timezone.now().date()
    this_month = timezone.now().replace(day=1).date()

    return [
        {
            "title": str(_("Contactos totales")),
            "value": Contact.objects.count(),
            "icon": "contacts",
            "description": str(_("Registrados en el sistema")),
        },
        {
            "title": str(_("Compras este mes")),
            "value": Purchase.objects.filter(date__gte=this_month).count(),
            "icon": "shopping_cart",
            "description": str(_("Desde el 1 del mes")),
        },
        {
            "title": str(_("Llamadas hoy")),
            "value": CallLog.objects.filter(date__date=today).count(),
            "icon": "call",
            "description": str(_("Del día de hoy")),
        },
        {
            "title": str(_("Emails hoy")),
            "value": EmailLog.objects.filter(date__date=today).count(),
            "icon": "email",
            "description": str(_("Del día de hoy")),
        },
    ]


def get_recent_contacts_table(request):
    from .models import Contact

    recent = Contact.objects.order_by("-created_at")[:5]
    return _Table(
        headers=[str(_("Nombre")), str(_("Email")), str(_("Estado")), str(_("Creado"))],
        rows=[
            [c.name, c.email, c.get_status_display(), c.created_at.strftime("%d/%m/%y")]
            for c in recent
        ],
    )
```

- [ ] **Step 2: Agregar entrada al sidebar en `config/settings/base.py`**

Localizar la sección `"navigation"` en `UNFOLD["SIDEBAR"]` y agregar la sección CRM Demo después del comentario `# --- Agregar secciones del proyecto aquí ---`:

```python
# --- Agregar secciones del proyecto aquí ---
{
    "title": _("CRM Demo"),
    "separator": True,
    "collapsible": True,
    "items": [
        {
            "title": _("Gestión de contactos"),
            "icon": "contacts",
            "link": "/admin/crm-demo/contacts/",
        },
        {
            "title": _("Contactos (admin)"),
            "icon": "manage_accounts",
            "link": "/admin/crm_demo/contact/",
        },
    ],
},
```

- [ ] **Step 3: Agregar cards al `dashboard_callback` en `apps/core/dashboard.py`**

Localizar la sección `context.update({"stat_cards": [...]})` y reemplazar el comentario `# PROYECTO: agregar stat cards específicas del proyecto aquí` con:

```python
# PROYECTO: agregar stat cards específicas del proyecto aquí
*_get_crm_cards_safe(request),
```

Y agregar la función helper al inicio del archivo (después de los imports existentes):

```python
def _get_crm_cards_safe(request):
    try:
        from apps.crm_demo.dashboard import get_crm_cards
        return get_crm_cards(request)
    except Exception:
        return []
```

- [ ] **Step 4: Verificar sistema sin errores**

```bash
docker compose -f docker-compose.dev.yml exec web_dev python manage.py check
```
Expected: `System check identified no issues`.

- [ ] **Step 5: Commit**

```bash
git add apps/crm_demo/dashboard.py config/settings/base.py apps/core/dashboard.py
git commit -m "feat(crm_demo): add dashboard widget and sidebar entry"
```

---

## Task 9: Tests de vistas completos

**Files:**
- Modify: `apps/crm_demo/tests/test_views.py`

- [ ] **Step 1: Completar `test_views.py` con todos los tests del spec**

```python
# apps/crm_demo/tests/test_views.py
import json
import pytest
from django.urls import reverse

from apps.crm_demo.models import Contact


@pytest.mark.django_db
def test_contact_list_requires_staff(client):
    url = reverse("crm_contact_list")
    response = client.get(url)
    assert response.status_code == 302


@pytest.mark.django_db
def test_contact_list_accessible_for_staff(admin_client):
    url = reverse("crm_contact_list")
    response = admin_client.get(url)
    assert response.status_code == 200


@pytest.mark.django_db
def test_contact_items_partial(admin_client):
    url = reverse("crm_contact_items")
    response = admin_client.get(url)
    assert response.status_code == 200


@pytest.mark.django_db
def test_contact_create_post_creates_contact(admin_client):
    url = reverse("crm_contact_create")
    data = {
        "name": "Laura",
        "email": "laura@test.com",
        "phone": "",
        "company": "ACME",
        "status": "lead",
    }
    response = admin_client.post(url, data)
    assert response.status_code == 204
    assert "HX-Trigger" in response
    trigger = json.loads(response["HX-Trigger"])
    assert "showToast" in trigger
    assert Contact.objects.filter(email="laura@test.com").exists()


@pytest.mark.django_db
def test_contact_inline_edit_patch(admin_client):
    contact = Contact.objects.create(name="Test", email="test@test.com", phone="")
    url = reverse("crm_contact_inline_edit", kwargs={"pk": contact.pk})
    response = admin_client.patch(
        url,
        data=json.dumps({"field": "phone", "value": "123456789", "field_type": "text"}),
        content_type="application/json",
    )
    assert response.status_code == 200
    assert "HX-Trigger" in response
    contact.refresh_from_db()
    assert contact.phone == "123456789"


@pytest.mark.django_db
def test_contact_delete(admin_client):
    contact = Contact.objects.create(name="Delete Me", email="del@test.com")
    url = reverse("crm_contact_delete", kwargs={"pk": contact.pk})
    response = admin_client.post(url)
    assert response.status_code == 204
    assert "HX-Redirect" in response
    assert not Contact.objects.filter(pk=contact.pk).exists()
```

- [ ] **Step 2: Ejecutar todos los tests**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/ -v
```
Expected: Todos los tests PASSED.

- [ ] **Step 3: Commit**

```bash
git add apps/crm_demo/tests/test_views.py
git commit -m "test(crm_demo): complete view tests — create, inline-edit, delete"
```

---

## Task 10: Smoke test manual + test suite completo

- [ ] **Step 1: Levantar el servidor**

```bash
docker compose -f docker-compose.dev.yml up -d
```

- [ ] **Step 2: Verificar flujo completo en el navegador**

1. Ir a `http://localhost:8000/admin/crm-demo/contacts/`
2. Verificar que carga `page_header`, `stat_cards`, `filter_tabs`, `split_panel`
3. Hacer click en un tab de filtro — verifica que `contact_list_items` se recarga
4. Hacer click en "Nuevo contacto" — verifica que `modal_content` abre con el form
5. Crear un contacto — verifica `toast` de éxito
6. Hacer click en un contacto de la lista — verifica que el panel derecho carga `data_list`, `copy_to_clipboard`, historiales con `empty_state`
7. Click en el ícono de edición en teléfono — verifica `inline_edit`
8. Guardar — verifica `toast` de éxito
9. Click en "Eliminar" en el detalle — verifica `confirm_dialog` en modo estricto
10. Confirmar eliminación — verifica `toast` y redirect

- [ ] **Step 3: Ejecutar suite completo**

```bash
docker compose -f docker-compose.dev.yml exec web_dev pytest apps/crm_demo/ -v --tb=short
```
Expected: Todos los tests PASSED, 0 errores.

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "feat(crm_demo): complete CRM demo app — all 12 UI components integrated"
```

---

## Resumen de tareas

| # | Tarea | Componentes UI cubiertos |
|---|-------|--------------------------|
| 1 | App skeleton + AppConfig | — |
| 2 | Models + migración | — |
| 3 | Data migration config | — |
| 4 | ContactService | — |
| 5 | Admin | — |
| 6 | URLs + vistas base | — |
| 7 | Templates | `page_header`, `stat_card`, `filter_tabs`, `split_panel`, `inline_edit`, `empty_state`, `skeleton`, `data_list`, `copy_to_clipboard`, `modal_content`, `toast`, `confirm_dialog` |
| 8 | Dashboard + sidebar | `stat_card` en dashboard |
| 9 | Tests de vistas completos | — |
| 10 | Smoke test manual | Verificación visual de los 12 |
